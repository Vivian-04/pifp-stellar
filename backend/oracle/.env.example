# PIFP Oracle Configuration
# Copy this file to .env and fill in your values

# ============================================
# Required Configuration
# ============================================

# Soroban RPC endpoint
RPC_URL=https://soroban-testnet.stellar.org

# PIFP contract address (starts with 'C')
CONTRACT_ID=CAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAD2KM

# Oracle's Stellar secret key (starts with 'S')
# SECURITY: Never commit this to version control!
ORACLE_SECRET_KEY=SAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA

# ============================================
# Optional Configuration (with defaults)
# ============================================

# Horizon API endpoint for transaction submission
HORIZON_URL=https://horizon-testnet.stellar.org

# IPFS gateway URL for fetching proof artifacts
IPFS_GATEWAY=https://ipfs.io

# Stellar network passphrase
NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Request timeout in seconds
TIMEOUT_SECS=30

# Optional: Sentry DSN for error tracking
# SENTRY_DSN=https://your-key@sentry.io/project-id

# Port for the health (/health) and metrics (/metrics) HTTP server
METRICS_PORT=9090

# ============================================
# Production Notes
# ============================================
# 
# For production deployments:
# 1. Use mainnet endpoints:
#    RPC_URL=https://soroban-mainnet.stellar.org
#    HORIZON_URL=https://horizon.stellar.org
#    NETWORK_PASSPHRASE=Public Global Stellar Network ; September 2015
#
# 2. Store ORACLE_SECRET_KEY in a secure vault
#    (HashiCorp Vault, AWS Secrets Manager, etc.)
#
# 3. Consider using a private RPC endpoint to avoid rate limits
#
# 4. Use a reliable IPFS gateway or run your own IPFS node
